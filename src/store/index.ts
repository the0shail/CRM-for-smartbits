import { createStore } from 'vuex'
import storeProduct from './modules/product'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    storeProduct
  }
})
